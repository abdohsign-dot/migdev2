# TODO
- [ ] Update `app.json` to remove `expo-splash-screen` plugin usage
- [ ] Update `app.json` splash image to `assets/ic_launcher.png` and use full-screen friendly resize mode
- [ ] Update `App.tsx` to remove runtime dependency on `expo-splash-screen` (preventAutoHide/hideAsync)
- [ ] Update `plugins/withSplashFill.js` to use `assets/ic_launcher.png` (not `assets/splash.png`)
- [ ] Prebuild/run Android to validate splash appears full-screen

